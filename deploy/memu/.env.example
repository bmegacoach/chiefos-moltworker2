# memU Environment Configuration
# Using Kimi K2.5 API (OpenAI-compatible)

# Kimi API Configuration (OpenAI-compatible endpoint)
OPENAI_API_KEY=${KIMI_API_KEY}
OPENAI_API_BASE=https://api.moonshot.cn/v1

# PostgreSQL Password (auto-generated, change if desired)
POSTGRES_PASSWORD=memu_secure_password
